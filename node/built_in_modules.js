// Export the function to other files that add these into it.
module.exports = 
{
    
};

// These are the required modules that are installed.
express = require('express');
bodyParser = require('body-parser');
multer = require('multer');
path = require('path');
fs = require('fs');

//This is a module that is used for the execution of this function.
nodemailer = require('nodemailer');

// Instantiate the express object
app = express(); 

